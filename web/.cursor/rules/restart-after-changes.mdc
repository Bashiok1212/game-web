---
description: 修改 web 项目后自动重启服务
globs: web/**/*
alwaysApply: false
---

# Web 项目修改后重启服务

修改 `/root/web` 下的代码后，应重启 Node 服务使改动生效。

**操作**：在项目根目录执行：
```bash
cd /root/web && pkill -f "node server.js" 2>/dev/null; sleep 1; npm start
```
或使用开发模式（文件变更自动重启）：
```bash
cd /root/web && npm run dev
```
